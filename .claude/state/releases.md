# Releases — Based prototype (owned by project-manager)

_Each milestone, once PO-accepted and `pnpm verify`-green, is committed + git-tagged +
deployed to staging by the project-manager working with dev-ops. Newest first._

## Log
| Date | Milestone | Tag | Commit | Staging verified | Notes |
|------|-----------|-----|--------|------------------|-------|
| 2026-06-04 | **Based TV (R1+R2) — YouTube _news_ Source end-to-end** (mid-M5 increment; ADR 0010) | _(none yet — see tag recommendation)_ | **`6d60b77`** (deployed) · **`2ca12c2`** (HEAD) | ✅ **DEPLOYED to staging + verified live (dev-ops, direct `infra/deploy-staging.sh`) — NOT pushed (navigator-gated).** FE CloudFront `https://d253xma588uo3l.cloudfront.net` `200`, **rebuilt bundle `index-Do1eTeUF.js`** (has `/sources/events` + the App Runner base URL baked in; CORS allows the CloudFront origin). BE `/health` `200`. App Runner `based-staging-backend` **RUNNING** on **fresh image `based-staging-backend:2ca12c2-20260604145608`**. **`GET /sources/events` `200` → 50 news `PerceptionEvent`s** from all 5 seeded channels (BBC/Reuters/AP/Sky/NBC), `youtube.com/embed/<id>` vantages, `type:'other'` (spoiler-safe), **NO API key in the response body**. CFN stack `based-staging` → UPDATE_COMPLETE. | **Status: committed (3 ahead of `origin/main`@`f272ba5`) + DEPLOYED to staging via direct `infra/deploy-staging.sh` + verified — NO push, NO tag yet (navigator-gated; see recommendations below).** **What shipped:** Based TV — the YouTube **news** Source, end-to-end behind a flag (ADR 0010, R1+R2 of M5; M5's R3–R6 still to come). **Backend:** a YouTube Data API source (live fetch; **official-embeds-only** skip of non-embeddable/age-restricted; **TTL cost-gating** cache; **secrets-from-env**; **failure-silent** → `[]` on any error) → `SourceRegistry` (`Promise.allSettled`) → **`GET /sources/events`**; `createNewsSources()` seeds the 5 channels; `server.ts` serves them. **Frontend:** `createRemoteSource` (failure-silent) + `resolveRegistry` behind **`VITE_USE_REMOTE_SOURCE`** + `main.tsx` wiring — **mock-fallback when the flag is off (default)**, so flag-off = exactly today's behavior = **no regression risk**. **Commits (3 ahead of `origin/main`@`f272ba5`; NOT pushed):** `176c23f` (pre-existing chore: team-tactics 0.10.0) · **`6d60b77`** `feat(based-tv): YouTube news Source end-to-end (R1+R2) behind VITE_USE_REMOTE_SOURCE` (the deployed product commit) · **`2ca12c2`** `chore: upgrade team-tactics to 0.11.1` (HEAD, harness-only). **Secret:** a NEW SSM SecureString `arn:…parameter/based/staging/youtube-api-key` → App Runner env `YOUTUBE_API_KEY` via the instance role (**mirrors the `GEMINI_API_KEY` posture exactly**); the browser **never holds it** — the fetch is server-side behind `/sources/events`, the bundle carries only the public `/sources/events` URL. **Verification (all green, dev-ops live 2026-06-04):** `/health` `200`; `GET /sources/events` `200` + **50 news `PerceptionEvent`s** across all 5 channels, youtube embeds, `type:'other'`, **no key in body**; FE bundle has `/sources/events` + the base URL baked; CORS allows the CloudFront origin; **no key in logs / bundle / tracked files**; secret-scan of the 3 unpushed commits (`f272ba5..HEAD`) **CLEAN** (no `AIza…`/`AKIA|ASIA…`/key-assignment/PEM; PM re-verified). **`pnpm verify`=0 (backend 27 / frontend 51**, typecheck+lint clean). **Bar/invariants — all four data-path guards PROVEN on the new YouTube source path** (ADR 0003/0010): **secrets-from-env** (`YOUTUBE_API_KEY` `process.env`-only; no-key→`[]`), **official-embeds-only** (`youtube.com/embed/<videoId>`; skip non-embeddable/age-restricted), **cost-gating** (per-source TTL cache + registry bounding), **failure-silent** (`fetch()`→`[]`, one dead source never sinks the feed). `tdd-critic` CONCERNS → CLOSED. **Deploy = DIRECT `infra/deploy-staging.sh`** (NOT CI/push): SSM SecureString seeded → CFN `based-staging` UPDATE_COMPLETE → App Runner image `2ca12c2-20260604145608` RUNNING → FE rebuilt → S3 → CloudFront invalidated (bundle `index-Do1eTeUF.js`). **Experiential accept = PENDING the navigator's watch-through** (qa browser tooling remains down — the navigator is the experiential proof, as with WDC/M4): open FE → flip to the real feed → confirm news moments render with official YouTube embeds, spoiler-safe rail labels, host stays in-budget. This verify is the HTTP surface (`/sources/events` `200` + 50 spoiler-safe events) + the byte-verified bundle + green `pnpm verify`. |
| 2026-06-04 | **M4 — two-level ranking + "while you were gone" digest** (the LAST unmet brief DoD #1) | **`m4`** (annotated, on `c699a1e`) | `c699a1e` | ✅ **PUSHED + CI-REDEPLOYED + verified** · FE CloudFront `https://d253xma588uo3l.cloudfront.net` `200`, live bundle **`index-DGjY9pqp.js`** (dev-ops byte-verified: `eventScore` ranker + the reworded digest `"Feed is heating up … closing in on a huge run …"`, no outcome leak) · live voice **ON** — `/live/session`, `BidiGenerateContent`, `access_token`, `AudioContext` all present in the bundle (CI did NOT drop `VITE_LIVE_VOICE`) · BE `/health` `200 {"status":"ok"}` · App Runner `based-staging-backend` **RUNNING** on **fresh image `f272ba5-20260604085759`** (CI rebuilt the backend too) · `POST /live/session` `200` w/ `token`+`setup` (live model `gemini-3.1-flash-live-preview`; token redacted) · digest **spoiler-safe** (the lone "world record" string in the bundle is the internal `narrative` DATA field, NEVER rendered — ADR 0009) | **Status: PUSHED to `origin` + annotated tag `m4` + CI-on-push REDEPLOYED (full FE+BE rebuild) + verified. 2026-06-04 (navigator: "Push now + tag m4").** M4 = a REAL two-level **`eventScore` ranker** (`rankFeed`/`computeEventScore` in `frontend/src/lib/ranker.ts`, weighted blend of heatDelta/novelty/legibility/confidenceTier) **replacing the M2 `heatDelta` placeholder**, + the **"while you were gone" digest** voiced FIRST on Start before the live feed, reworded spoiler-safe. PO KICKOFF (`design-notes.md`); **NO architect** — additive on existing contracts (`RankedFeed.eventScore` + `HostDirective.action:'digest'`). `pnpm verify`=0 (**frontend 46 / backend 16**, e2e 1 skipped, typecheck+lint clean) re-confirmed at push; critic CONCERNS → CLOSED (voiced-digest spoiler guard + multi-event cost-gating tripwire + `character.test` digest-render). **Push:** `origin/main` `ce194e7 → f272ba5` (17-commit range = M4 `c699a1e` + WDC/LV1 + team-tactics chores 0.4.0→0.9.2 + the `docs(state)` commit `f272ba5`); annotated **`m4` on `c699a1e`** (`refs/tags/m4^{}`→`c699a1e`, verified via `git ls-remote`; nothing extra pushed). **FINAL secrets-scan of the whole push range = CLEAN** — no `AIza…`/`AKIA|ASIA…`/PEM/`.env` contents/real token values across all 17 commits; `backend/.env`+`frontend/.env.local`+`backend/scripts/` NEVER committed in any history (only `backend/.env.example` tracked); `deploy-staging.sh` has NO hardcoded key (sources from `backend/.env`→SSM, only the ARN reaches CFN). The token-shaped diff strings are documented test-fixture stubs (`ephemeral-abc-123`, …) + ADR prose. **Deploy = CI-on-push** (run [`26941700459`](https://github.com/geda0/Based/actions/runs/26941700459), **success**, 5m7s): `pnpm verify` gate → green → `infra/deploy-staging.sh` → **full rebuild** (BE image `f272ba5-…` → App Runner; FE rebuilt w/ `VITE_API_BASE_URL`+**`VITE_LIVE_VOICE=1`** baked in by the script's step 4 → S3/CloudFront, invalidated). ⚠ **Live-voice flag mechanism (the key risk, RESOLVED):** CI runs `infra/deploy-staging.sh`, whose step-4 FE build hardcodes `VITE_LIVE_VOICE=1` — so CI does NOT depend on any GH-stored flag and could NOT silently drop live voice; byte-verified ON in the live bundle. `GEMINI_MODEL=gemini-3.1-flash-lite` (IaC default via CI); SSM key ARN `arn:aws:ssm:us-east-2:429844072978:parameter/based/staging/gemini-api-key` (value hidden). **Residual experience check (navigator's eye/ear only):** the catch-up watch-through (open FE → Start → host delivers the digest, THEN ranked live moments) + the §13 audible — this verify is the HTTP surface + the byte-verified bundle + green CI. **All brief DoDs complete on PO accept.** |
| 2026-06-03 | LV1 — live-voice host (BROWSER-DIRECT Gemini Live audio) | `lv1` (re-tagged) | `9b07c38` | ✅ **DEPLOYED + verified** · FE CloudFront `200` · BE `/health` `200 {"status":"ok"}` · **`POST /live/session` `200` returning `{ token, model, expiresAt, setup }` — the `setup` envelope PRESENT** (`{ setup: { model:"models/gemini-3.1-flash-live-preview", generationConfig:{responseModalities:["AUDIO"]}, systemInstruction:{…no-spoiler…} } }`; token redacted) · App Runner `based-staging-backend` **RUNNING** on the new image `9b07c38-20260603160335` · CORS allows the CloudFront origin on `/live/session` (preflight `204` + POST `200`, `access-control-allow-origin: https://d253xma588uo3l.cloudfront.net`) | **Status: committed + re-tagged + DEPLOYED to staging (no push — navigator-gated).** This is the **browser-direct** LV1 (ADR 0007 Amendment C) — it **SUPERSEDES** the relay-based `78b1d9f` previously tagged `lv1` (the tag was moved to the browser-direct HEAD). PO **re-accepted** on the browser-direct transport 2026-06-03; `pnpm verify`=0 at the tag (**backend 16, frontend 37**, e2e 1 skipped, typecheck+lint clean). **Annotated tag `lv1`** re-pointed `78b1d9f → 9b07c38` (`git tag -d lv1 && git tag -a lv1 -m "LV1 — live-voice host (browser-direct Gemini Live audio)"`). **Secrets gate CLEAN:** `backend/.env` (real `GEMINI_API_KEY`) gitignored + unstaged; no `frontend/.env.local`; only `backend/.env.example` tracked; staged-diff secret grep (`AIza`/`GEMINI_API_KEY=`/`access_token=<val>`/`auth_tokens/`) found **no real values** on either commit (the one hit was the **removed** test-fixture line `access_token=eph-123`, a stub; the `?access_token=`/`auth_tokens/…` URL-template strings are the feature); throwaway probes `backend/scripts/` gitignored. **Two commits on top of the relay-based `78b1d9f`:** (A) `053591f` `chore: upgrade teamentic to 0.5.0` (harness/kit/docs delta — `tdd-pairing→teamentic` markers, `.teamentic/` manifest, upgraded hooks, the new planner state file, + the 0.4.0 "Flagged" leftovers `.claude/tdd.config` / `.github/workflows/tdd-verify.yml` / `.claude/state/.gitkeep`); (B) `9b07c38` `refactor(lv1): browser-direct voice transport — retire relay + ECS` — `/live/session` now ALSO returns the server-built `setup` envelope (`live.routes.ts`); **DELETED** `live-relay.ts`/`live-relay.routes.ts`/`live-relay.test.ts` + removed the `@fastify/websocket`/`ws`/`@types/ws` deps (`package.json` + `pnpm-lock.yaml`); FE `live-relay-client.ts` opens Google's Live WSS **directly** (`…BidiGenerateContentConstrained?access_token=`, no relay, `binaryType='arraybuffer'`); `live-narrator.ts` sends **setup→clientContent** on `open` (cycle-7 inverted); ADR 0007 **Amendment C** + state docs. **Topology a** (browser → Google Live WSS direct): the browser mints via `/live/session`, opens Google's WSS with the server-minted ephemeral token, and sends the canonical server-built `setup` itself; the long-lived `GEMINI_API_KEY` stays SSM/`process.env`-only, server-side (only the short-lived single-use ~3-min token transits — A1's accepted posture). **Relay + ECS RETIRED/CANCELLED** (saves ~$25–30/mo + the standup; App Runner keeps only the plain-HTTP mint — it never could host WS, which is now fine). **Live voice ON in staging** via `VITE_LIVE_VOICE=1` (a non-secret build flag added to `deploy-staging.sh` step 4; **no `VITE_LIVE_RELAY_URL`** — retired). Live model `gemini-3.1-flash-live-preview` ships via the code default (`GEMINI_LIVE_MODEL` knob; NOT the App Runner `GEMINI_MODEL`, which stays `gemini-3.1-flash-lite` for the unchanged `/narrate` text path). SSM key ARN `arn:aws:ssm:us-east-2:429844072978:parameter/based/staging/gemini-api-key` (value hidden). **Deployed via `infra/deploy-staging.sh`** (Docker up; image build/push → App Runner, FE build w/ `VITE_LIVE_VOICE=1` → S3/CloudFront, invalidate). **Residual:** the §13 **audible / voice-identity** confirmation is the navigator's ear at the staging URL (a browser check — the live-voice AUDIO itself; this verify is the HTTP surface + that the mint returns `setup`). **Not pushed** (push waits for the navigator). |
| 2026-06-03 | post-m3 fix · embeds + CI single-source | _(none — patch between `m3` and LV1)_ | `ce194e7` | ✅ FE CloudFront 200 · BE `/health` 200 · App Runner `based-staging-backend` RUNNING (image `based-staging-backend:ce194e7-20260603041536`) · **`GEMINI_MODEL`=`gemini-3.1-flash-lite` shipped by CI** (no manual deploy — the CI single-source gap is closed) · live `POST /narrate` smoke 200 (real spoiler-safe line: _"Co-streamer A is holding his breath as the final duel kicks off—chat is absolutely losing it right now!"_) · embed fixes confirmed in the deployed bundle (Twitch `parent`, rail `type · streamer`) | Patch (no milestone tag) between `m3` and the upcoming **LV1** cut. **Commits:** `a66fc29` (embed fixes + ADRs 0007/0008/0009 + ADR 0003 amendment + state docs + first CI fix) and `ce194e7` (CFN always-pass correction); **HEAD=`ce194e7`**, pushed to `origin/main`, no new tag. **Embed fixes:** Twitch `parent` param correct so the player loads on the staging host; discovery-rail labels are spoiler-safe (`type · streamer`, no outcomes). **CI `GEMINI_MODEL` single-source gap CLOSED:** root cause was CFN — on a stack *update*, an **omitted** parameter retains its **stale** prior value (not the template default), so CI kept re-shipping `gemini-3.1-flash` despite the template. Fix: `GEMINI_MODEL` is now the **version-controlled default in `infra/staging.yaml`** (`gemini-3.1-flash-lite`) **and** `deploy-staging.sh` **always** passes it to CloudFormation — so CI ships the correct model with **no reliance on gitignored `backend/.env`**. Proven live (gap closed). **Deploy:** via CI ([`deploy-staging.yml` run `26863305091`](https://github.com/geda0/Based/actions/runs/26863305091)) — `pnpm verify` gate → green → deploy; **no manual `deploy-staging.sh`, no `UPDATE_IN_PROGRESS` collision**. (Note: the earlier `docs(releases): record m3` commit `ad49d56` had a CI deploy that **silently re-broke** the model to `gemini-3.1-flash`, overwriting m3's manual correction — which is why the prior "Staging (current)" snapshot was stale; this fix repaired it **and** prevents recurrence.) |
| 2026-06-02 | M3 — real Gemini narration | `m3` | `217db0b` | ✅ FE CloudFront 200 · BE `/health` 200 `{"status":"ok"}` · App Runner `based-staging-backend` RUNNING (image `based-staging-backend:217db0b-…`, has `/narrate`) · **`GEMINI_MODEL` corrected → `gemini-3.1-flash-lite`** (was the wrong 404 `gemini-3.1-flash`) · live `POST /narrate` smoke 200 (real spoiler-safe tier-hedged line) | `feat(m3): real Gemini narration — /narrate proxy + cost-gated FE integration`. PO-accepted 2026-06-02; **annotated tag `m3`** on the green milestone commit; `pnpm verify`=0 (**backend 7, frontend 24**, e2e skipped). **Pushed to `origin`** (github.com/geda0/Based): `refs/heads/main`=`217db0b` + annotated `refs/tags/m3`→`217db0b` (clean fast-forward over `0331221`; nothing extra). Secret-safe (only `backend/.env.example` tracked, key blank; real `.env` gitignored — `git status` secret-free pre-commit; key never printed). **Deploy:** the push auto-triggered CI run [`26861017913`](https://github.com/geda0/Based/actions/runs/26861017913) (`pnpm verify` gate → green → `infra/deploy-staging.sh`, **success**, sha `217db0b`) — but **CI deployed the wrong `GEMINI_MODEL` (`gemini-3.1-flash`)** because CI has no `backend/.env` to source, so the script fell back to its default. A manual `infra/deploy-staging.sh` run (local `backend/.env`=`gemini-3.1-flash-lite`) **corrected it** — first attempt collided with the in-progress CI deploy (`UPDATE_IN_PROGRESS`, exit 254, no harm: SSM key + image already refreshed); re-run after the stack settled landed `GeminiModel=gemini-3.1-flash-lite` (verified on App Runner). SSM key ARN `arn:aws:ssm:us-east-2:429844072978:parameter/based/staging/gemini-api-key` (value hidden). FE built with `VITE_API_BASE_URL`=the App Runner URL so the SPA reaches `/narrate`. **Known CI gap → follow-up below.** |
| 2026-06-02 | M0–M2 | `m2` | `eb715a6` | ✅ FE CloudFront 200 · BE `/health` 200 `{"status":"ok"}` · App Runner `based-staging-backend` RUNNING | Initial commit (`main` had 0 commits) folds M0a/M0b/M1/M2 — contracts, channel-surf shell, silent↔active host loop + staging infra. Annotated tag, green bar (`pnpm verify`=0), secret-safe (only `backend/.env.example` tracked; real `.env` gitignored). **Pushed to `origin`** (github.com/geda0/Based) 2026-06-02: branch `main` (upstream set, `refs/heads/main`=`eb715a6`) + annotated tag `m2` (`refs/tags/m2`→`eb715a6`); no auth issues, nothing pushed beyond `main`+`m2`. 102 files. |

## Staging (current — Based TV R1+R2 on top of M4; commit `6d60b77`; DEPLOYED via direct `deploy-staging.sh`, NOT pushed — `origin/main`=`f272ba5`)
- **Frontend (Based TV R1+R2):** https://d253xma588uo3l.cloudfront.net  (S3 private + CloudFront/OAC) — **rebuilt SPA bundle `index-Do1eTeUF.js`** with the **Based TV remote source** (`createRemoteSource`+`resolveRegistry`, behind **`VITE_USE_REMOTE_SOURCE`**) calling **`GET /sources/events`** (the App Runner base URL baked in), **mock-fallback when the flag is off** (default → exactly the M4 behavior, no regression). Still carries the M4 surface beneath it: the **two-level `eventScore` ranker** + the **"while you were gone" digest**, `VITE_API_BASE_URL`=the backend URL, **`VITE_LIVE_VOICE=1`** (browser-direct Gemini Live audio — `/live/session`+`BidiGenerateContent`+`access_token`+`AudioContext`). Reaches `/sources/events` (Based TV) + `/narrate` (text) + `/live/session` (mint); embed fixes live (Twitch `parent`, YouTube `youtube.com/embed`, spoiler-safe rail labels). CloudFront `200` (dev-ops verified 2026-06-04); CORS allows the CloudFront origin on `/sources/events`.
- **Backend (Based TV R1+R2):**  https://pmf6qpvkfx.us-east-2.awsapprunner.com  (App Runner; `/health` `200 {"status":"ok"}`) — **fresh image `based-staging-backend:2ca12c2-20260604145608`** (direct-deploy rebuild; commit `6d60b77` product) now serving **`GET /sources/events`** (`SourceRegistry` over `createNewsSources()` → 5 seeded YouTube news channels; `200` → **50 `PerceptionEvent`s**, youtube embeds, `type:'other'`, **no key in body**) **plus** the prior `POST /narrate` + `POST /live/session` (mint returns `{ token, model, expiresAt, setup }`). **`GEMINI_MODEL`=`gemini-3.1-flash-lite`** (text path, IaC default); live model `gemini-3.1-flash-live-preview` via the `GEMINI_LIVE_MODEL` code default. **Secrets via SSM SecureString:** Gemini key `arn:…parameter/based/staging/gemini-api-key` **+ NEW** YouTube key `arn:…parameter/based/staging/youtube-api-key` → App Runner env `YOUTUBE_API_KEY` via the instance role (mirrors GEMINI; values hidden, browser never holds them). CORS_ORIGINS = the CloudFront origin. **No WSS relay on App Runner** — the live path is browser→Google-direct; the Based TV fetch is server-side behind `/sources/events`.
- **Repo vs staging:** the live FE + backend = the **Based TV commit `6d60b77`** (NOT yet tagged, NOT yet pushed). **`origin/main`=`f272ba5`** (the M4 `docs(state)` commit). Local **HEAD=`2ca12c2`** is **3 ahead of `origin/main`**: `176c23f` (team-tactics 0.10.0 chore) + `6d60b77` (Based TV R1+R2 — the deployed product) + `2ca12c2` (team-tactics 0.11.1 chore, harness-only, NOT deployed). Staging runs `6d60b77`'s code; `2ca12c2` is a harness/kit bump on top with no product delta. M4 (`c699a1e`, tag `m4`) remains the last **tagged** milestone, pushed (see the M4 row + Resolved).
- Region **us-east-2** · stacks `based-staging-ecr`, `based-staging` (UPDATE_COMPLETE) · deploy: **DIRECT `infra/deploy-staging.sh`** for this cut (NOT CI/push — Based TV is held unpushed pending the navigator). CI on push to `main` (`deploy-staging.yml`) remains the routine path once pushed; `deploy-staging.sh` is self-sufficient for Based TV (reuses SSM for the YouTube key + carries the flags), so a future push would CI-redeploy cleanly. The CI `GEMINI_MODEL` gap is **resolved** (IaC default). **ECS Express Mode CANCELLED** — no persistent-socket host needed.

## CI/CD (live as of 2026-06-03)
Deploys to staging are now **automated on push to `main`** — previously a manual
`infra/deploy-staging.sh` run. **Keyless** (no stored AWS keys) and **gated by `pnpm verify`**.
- **Workflow:** `.github/workflows/deploy-staging.yml` — on push to `main` (or manual
  `workflow_dispatch`); `concurrency: deploy-staging` serializes deploys.
- **Auth:** GitHub **OIDC** → assumes IAM role `based-staging-ci-deploy`
  (account `429844072978`, region `us-east-2`). No secrets in GitHub; the Gemini key
  stays in SSM (reused by the deploy script). Infra: `infra/ci-oidc.yaml` (stack `based-staging-ci`).
- **Gate:** the job runs `pnpm verify` (typecheck + lint + full suite) before deploying —
  a red bar never reaches staging.
- **Verified end-to-end (dev-ops):** run
  https://github.com/geda0/Based/actions/runs/26857612273 → **success**; staging
  redeployed (fresh image `based-staging-backend:0331221-20260603011137`), health **3/3** green.
- **CI commits on `main` (pushed, not a milestone — no tag):** `461c1d0` (workflow) +
  `0331221` (pnpm-version fix, derives pnpm from `packageManager`). In-flight M3 work was
  uncommitted, so CI deployed the **m2+CI** state.

## Cadence (go-forward — CI/CD model as of 2026-06-03)
- **Release per milestone, in parallel with feature work.** The go-forward flow:
  **PM gates** (PO-accepted **and** `pnpm verify` green, no unrelated mid-edit work) →
  **dev-ops commits + annotated `mN` tag** → **push `main`** → **CI auto-deploys + verifies**.
  No manual deploy for routine releases — the push **is** the deploy.
- **Deploy/verify is now CI's job.** Pushing `main` triggers
  `.github/workflows/deploy-staging.yml`: `pnpm verify` gate → `infra/deploy-staging.sh` →
  health. The PM **confirms the Actions run is green** (and health 3/3) rather than running
  the deploy by hand. `infra/deploy-staging.sh` remains the **manual/break-glass** path
  (run locally with AWS creds) for when CI is down or an out-of-band deploy is needed.
- **Push is part of every cut (navigator-approved 2026-06-02).** The milestone commit on
  `main` + the `mN` tag push to github.com/geda0/Based as part of the release — no longer
  a per-release navigator decision. Pushing `main` now also **triggers the deploy**.
- **Boundary gate (unchanged):** never push a red bar or a tree with unrelated mid-edit
  work (e.g. a parallel M3 session mid-cycle) — note CI deploys **whatever is on `main`**,
  so the gate matters more than ever. Wait for a clean boundary or coordinate. (Annotated
  tags don't trigger CI; the **push to `main`** does.)
- **Model-id changes are now CI-safe (single source of truth).** `GEMINI_MODEL` lives as the
  version-controlled default in **`infra/staging.yaml`**, and `deploy-staging.sh` **always**
  passes it to CloudFormation — so CI ships the correct model with no reliance on gitignored
  `backend/.env` and **no manual `infra/deploy-staging.sh` after the push**. To change the model
  id (e.g. LV1's `gemini-3.1-flash-live-preview`), **just update the `staging.yaml` default** in
  the milestone commit; the push deploys it. (Resolved 2026-06-03 — see the post-m3 fix row.)
- **Next:** **M4 is shipped + tagged `m4` + pushed** (all brief DoDs complete). The current in-flight
  work is **Based TV / M5** (ADR 0010 — a Source producer layer beneath §6): **R1+R2 (the YouTube news
  Source end-to-end) is DEPLOYED to staging** (commit `6d60b77`, direct `deploy-staging.sh`, behind
  `VITE_USE_REMOTE_SOURCE` with mock-fallback) but is a **mid-M5 increment — NOT yet tagged, NOT yet
  pushed** (recorded by log row, per the mid-milestone-increment convention; experiential accept pending
  the navigator's watch-through). **The next milestone cut is full M5 / Based TV** once R3–R6 (Twitch-as-a-
  Source, cross-platform feeds, the rest of the roster) land and the PO accepts — **then** cut the annotated
  `m5` tag on its green commit + push (CI redeploys; `deploy-staging.sh` already reuses SSM for `YOUTUBE_API_KEY`
  and bakes the flags, so no manual step). Same pattern each milestone.

## Navigator decisions
### Open (as of Based TV R1+R2, 2026-06-04)
- **Tag the Based TV (R1+R2) increment now, or wait for full M5 (R3–R6)?** — **OPEN / navigator + PM call.**
  Based TV R1+R2 (YouTube news Source end-to-end, ADR 0010) is a real, demoable, invariant-proven increment,
  but it is **mid-M5** — M5/Based TV's R3–R6 (Twitch-as-a-Source refactor, cross-platform feeds, the rest of
  the source roster) are still ahead. **PM recommendation: do NOT cut a milestone `mN` tag yet — wait for full
  M5.** Rationale: the `mN` convention is **one tag per completed milestone on its green commit**; tagging a
  partial M5 now would either (a) burn the `m5` name on an incomplete milestone, or (b) create an off-convention
  `m5-based-tv`-style increment tag we'd then have to reconcile against the real `m5`. R1+R2 is fully **recorded**
  here (commit `6d60b77`, image, endpoint, invariants) — the log row, not a git tag, is the durable marker for a
  mid-milestone increment (same as the post-m3 patch row, which is recorded-but-untagged by convention). If the
  navigator wants a **lightweight checkpoint** anyway, an annotated `based-tv-r2` (descriptive, NOT an `mN`
  milestone tag) on `6d60b77` is the clean option — but the default recommendation is to hold the tag for `m5`.
- **Push the 3 unpushed commits to `origin` (would CI-redeploy)?** — **OPEN / the navigator's call.** `origin/main`
  =`f272ba5`; local HEAD=`2ca12c2` is **3 ahead** (`176c23f` chore + `6d60b77` Based TV + `2ca12c2` chore).
  **PM recommendation: this is SAFE to push — recommend pushing, navigator's discretion on timing.** Why low-risk:
  (1) `deploy-staging.sh` is **self-sufficient under CI** — it reuses the SSM YouTube SecureString (no new
  GH-stored secret needed) and bakes `VITE_USE_REMOTE_SOURCE`/the flags itself, so a CI-on-push deploy would
  reproduce the direct deploy faithfully; (2) Based TV is **behind a flag with mock-fallback** → flag-off (default)
  = exactly today's M4 behavior = **no regression risk** even if the remote feed were to fail; (3) `pnpm verify`=0
  gates the push (backend 27 / frontend 51), and the secret-scan of `f272ba5..HEAD` is **CLEAN**. ⚠ A push **will**
  trigger `deploy-staging.yml` (FE+BE rebuild of whatever is on `main`=`2ca12c2`); since `2ca12c2` is a harness-only
  chore over `6d60b77`, the redeployed product is byte-equivalent to what's live. **Historically pushes have been
  navigator-gated** (the WDC→LV1→M4 run was held, then cleared with "Push now"), so surfacing this rather than
  auto-pushing. **The experiential accept (navigator's watch-through) is the only thing still pending** — a push is
  orthogonal to it (mock-fallback means a push can't degrade the experience).

### Resolved
- **M4 (`m4`/`c699a1e`) — tagged + pushed + CI-redeployed.** ✅ **DONE (2026-06-04, navigator: "Push now + tag m4").**
  The earlier "Open" items (push the WDC→LV1→M4 run / tag M4) are **closed**: annotated tag **`m4`** is on `c699a1e`
  **and pushed** (`git ls-remote origin` → `refs/tags/m4^{}`→`c699a1e`; PM verified), and **`origin/main`=`f272ba5`**
  (the M4 `docs(state)` commit). CI-on-push redeployed the full FE+BE (run `26941700459`, success); staging ran M4
  until the Based TV direct deploy layered R1+R2 on top. M4 = the last unmet brief DoD #1 — **all brief DoDs complete.**
- **Push to `origin`** — ✅ **Approved 2026-06-02** (standing approval). `m2`/`m3`/`m4` pushed to
  github.com/geda0/Based (branch `main` + the annotated `mN` tag) by dev-ops. Standing release flow
  (see Cadence / Conventions). The **Based TV R1+R2** commits are **held unpushed** pending the navigator (see Open
  above) — a mid-M5 increment, not a milestone cut; push resumes when cleared.

## Follow-ups (non-blocking — owner-routed, not release blockers)
- **✅ RESOLVED 2026-06-03 — CI deployed a stale `GEMINI_MODEL` (dev-ops).** _Was: surfaced during
  the m3 cut 2026-06-02; CI re-shipped the wrong model because it had no `backend/.env` to source._
  **Root cause** turned out to be CloudFormation: on a stack *update* an **omitted** parameter
  retains its **stale** prior value (not the template default), so CI kept re-applying
  `gemini-3.1-flash`. **Fix (shipped, `ce194e7`):** `GEMINI_MODEL` is the version-controlled default
  in **`infra/staging.yaml`** (`gemini-3.1-flash-lite`) **and** `deploy-staging.sh` **always** passes
  it to CFN — single source of truth, CI-safe, no reliance on `backend/.env`. Proven live (CI run
  `26863305091`). Future model-id changes just update the `staging.yaml` default (see Cadence). No
  manual deploy step remains.
- **✅ RESOLVED — ADR for the CI/CD decision (architect).** Recorded as part of the post-m3 fix
  (commit `a66fc29` landed ADRs 0007/0008/0009 + an ADR 0003 amendment). Captures the
  GitHub Actions + OIDC, deploy-on-push-to-`main` staging delivery model alongside ADR 0005.
- **Bump GitHub Action versions (dev-ops) before 2026-06-16.** GitHub's **Node 20→24
  runner cutover** lands 2026-06-16; bump the pinned actions in
  `.github/workflows/deploy-staging.yml` — `actions/checkout`, `actions/setup-node`,
  `pnpm/action-setup`, `aws-actions/configure-aws-credentials` (all `@v4` today) — to the
  Node-24-compatible majors ahead of the cutover so CI/deploys don't break.

## Conventions
- **Tags:** milestone tags `mN` (`m0`,`m1`,`m2`,…), **annotated**, on the green commit
  that completes the milestone. (Switch to semver `v0.N` if the navigator prefers.)
  **Patches/fixes between milestones are recorded but not tagged** (e.g. the 2026-06-03
  post-m3 embed + CI fix, `ce194e7`).
- **Model id is IaC, not env.** `GEMINI_MODEL` is the version-controlled default in
  `infra/staging.yaml`; CI ships it (no `backend/.env` dependency). Change the model id
  by editing that default in the milestone commit — the push deploys it, no manual step.
- **Commits:** Conventional Commits (`docs/conventions.md`), one milestone per release commit.
- **Push:** standard part of every release (navigator-approved 2026-06-02) — push the
  milestone commit on `main` + the `mN` tag to `origin` (github.com/geda0/Based) as part
  of the cut. dev-ops verifies via `git ls-remote` that `refs/heads/main` + `refs/tags/mN`
  landed and nothing extra was pushed.
- **Secrets:** `.env`/`backend/.env` are gitignored — dev-ops verifies `git status` is secret-free before committing.
