node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# Secrets — environment files are never committed (the example is).
.env
.env.*
!.env.example

# Throwaway live-API probes (read GEMINI_API_KEY from env; never committed).
backend/scripts/

# Transient TDD artifacts (derived per run — never commit). The OTHER state files
# (plan.md / progress.md / design-notes.md / backlog.md / releases.md / phase / layer)
# ARE committed for continuity.
.claude/state/suite-status
.claude/state/telemetry.jsonl

# tdd-pairing merge-scratch sidecars (review, then merge into the real file).
*.tdd-pairing.*

# Playwright
playwright-report/
test-results/

# OS / editor
.DS_Store
*.local

# >>> team-tactics (managed) >>>
# Transient kit artifacts - never commit (other state files ARE committed for continuity).
.claude/state/suite-status
.claude/state/telemetry.jsonl
.claude/state/tics.jsonl
.claude/state/tics.d/
.claude/**/*.bak
*.team-tactics.*
# <<< team-tactics (managed) <<<
